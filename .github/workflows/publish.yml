name: Publish to pub.dev

on:
  push:
    tags:
      - 'cached_video_thumbnail-v*'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    permissions:
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
